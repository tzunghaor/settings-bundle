tzunghaor_settings:
  scopes:
    - name: root
      children:
        - name: day
          children:
            - name: morning
            - name: afternoon
        - name: night

  default_scope: root

  mappings:
    default:
      dir: '%kernel.project_dir%/Settings'
      prefix: Tzunghaor\SettingsBundle\Tests\TestProject\Settings\